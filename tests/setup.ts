/**
 * @file Shared Bun test setup.
 *
 * Bun loads this module before the repository test suite. It is intentionally
 * empty for now, but remains the central place for future cross-test fixtures
 * that must apply to both package-surface tests and Oxlint plugin behaviour.
 */
